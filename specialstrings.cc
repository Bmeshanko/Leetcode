#include <bits/stdc++.h>
using namespace std;

class Solution {
public:

    void sort_chars(string word) {
        
    }

    pair<string, string> print_groups(string word) {
    string even;
    string odd;
    for (int i = 0; i < word.length(); i++) {

    }

    int numSpecialEquivGroups(vector<string>& words) {
        map<pair<string, string>> sorted_groups;

    }
};